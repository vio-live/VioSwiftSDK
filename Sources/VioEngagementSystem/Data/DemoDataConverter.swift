import Foundation
import VioCore

/// Converter for transforming timeline events to engagement models
/// Used in demo mode to convert TimelineDataGenerator events to Poll/Contest models
/// Note: Actual conversion logic is provided via closures from the demo app
/// since event types are defined in the demo, not the SDK
public struct DemoDataConverter {
    // This struct is kept for organizational purposes
    // Actual conversion happens via closures set in DemoEngagementRepository
}
